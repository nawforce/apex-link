project:
    name: CampaignTools
    package:
        name: Campaign Tools
        namespace: camptools
        api_version: 37.0
    git:
        prefix_release: rel/

flows:
    release_beta:
        tasks:
            5:
                task: mrbelvedere_publish
                options:
                    tag: ^^github_release.tag_name
