project:
    name: Relationships
    package:
        name: Relationships
        namespace: npe4
        api_version: 36.0
    git:
        release: rel/

flows:
    release_beta:
        tasks:
            # Ignore failures on release notes generation
            3:
                ignore_failure: True
