project:
    name: Recurring_Donations
    package:
        name: Recurring Donations
        namespace: npe03
        api_version: 36.0
    git:
        prefix_release: rel/

flows:
    release_beta:
        tasks:
            # Ignore failures on release notes generation
            3:
                ignore_failure: True
